# Build configuration for userapp2 (identical shape to userapp).
[build]
target = "x86_64-pc-windows-msvc"

[unstable]
build-std = ["core", "compiler_builtins"]
build-std-features = ["compiler-builtins-mem"]

[target.x86_64-pc-windows-msvc]
linker = "lld-link"
